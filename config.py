# Job-type taxonomy user configs and company records use. Kept here, not in
# sources/, since the dashboard Lambda packages config.py but not sources/.
SUPPORTED_JOB_TYPES = ["intern", "newgrad", "fulltime"]
